{
  "name": "feedloom",
  "version": "1.0.0",
  "description": "Turn a folder of MP3s into a self-hosted podcast — RSS feed and landing page, no hosting fees.",
  "bin": { "feedloom": "bin/feedloom.js" },
  "scripts": {
    "test": "node --test"
  },
  "engines": { "node": ">=18" },
  "license": "MIT"
}
